# v0.9.01-pre6

## Summary

This pre-release makes split tunneling more resilient when the app list cannot be read.

## User-facing changes

- Exact package rules keep working even if installed-app access is unavailable.
- Split tunneling fails more safely instead of producing confusing partial matches.

## Technical notes

- Rules that need installed-app inventory are skipped when that inventory is missing.
