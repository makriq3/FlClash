# v0.9.01-pre1

## Summary

This pre-release introduces Android split tunneling controlled directly by profile YAML.

## User-facing changes

- Added `tun.exclude-package` and `tun.include-package`.
- Let one profile define which Android apps should bypass or use the VPN.

## Technical notes

- Split tunneling rules are derived from the active profile instead of device-only settings.
